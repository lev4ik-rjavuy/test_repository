# ІДЗ

## Як запустити

1. Встановіть залежності: `make install`
2. Запустіть додаток: `make run`
3. Для запуску тестів: `make test`

## Налаштування Git Hooks (Pre-commit)

Для забезпечення єдиного стилю коду ми використовуємо `black` через `pre-commit`.
